# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# .NET build output
bin/
obj/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Directus (local authoring stack)
directus/.env
directus/data/
directus/uploads/

# Playwright (produced under client/, or at repo root via the pw.sh container mount)
test-results/
playwright-report/
blob-report/
playwright/.cache/
.pnpm/
# Container artefacts (HOME=/work in pw.sh, /work == repo root)
.cache/
.local/
# .config/ is ignored for container HOME droppings — if a dotnet tool-manifest is ever added, whitelist it: !.config/dotnet-tools.json
.config/
.fontconfig/
