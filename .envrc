# Auto-load decrypted secrets when present (sops + age). First time: run `direnv allow`.
if [ -f .env.sops ]; then eval "$(sops -d --input-type dotenv --output-type dotenv .env.sops | sed 's/^/export /')"; fi
